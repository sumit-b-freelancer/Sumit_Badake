const nextConfig = {
  output: 'export',
  basePath: '/my_portfolio',
  images: {
    unoptimized: true,
  },
  reactStrictMode: true,
};

export default nextConfig;
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
const nextConfig = {
  // config options here
};

export default nextConfig;